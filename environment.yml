name: opencpm
channels: [conda-forge, defaults]
dependencies:
  - python=3.8
  - pip
  - numpy
  - scipy
  - matplotlib
  - pip:
      - .
      - -e ./OpenCDA
      # later: OtherSub @ file://./OtherSub
